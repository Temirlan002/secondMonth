const a = "20" //string or text
const b = 20 // number

let c = "Sanjar"

const obj = {
    key: "value",//клю ч и значение
    obj2: {
        dota: 77,
        kostyl: "Jalil"
    }
}


// console.log(typeof a)
// console.log(obj.obj2.kostyl);

const arr = ["Aidana", "Adinay", "Sanjar", "Jalil"]
console.log(arr[3]);

const first_number = prompt("первое число?")
const last_number = prompt("второе число?")

if (first_number > last_number){
    console.log(first_number + ">" + last_number)
};

const otvet = alert("за домашку 10")